{
  "scripts": {
    "start:prod": "npm run build && node build/index.js",
    "start:dev": "npx nodemon",
    "build": "rimraf ./build && tsc"
  },
  "dependencies": {
    "@types/node": "^20.10.1",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.2"
  }
}
